export interface params {
  search: string;
  order: string;
  ascDesc: boolean;
  platforms: number;
}

export interface fullParams {
  search: string;
  order: string;
  ascDesc: boolean;
  platforms: number;
  page: number;
}
